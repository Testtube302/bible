{
  "name": "scripture-bible-pwa",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "scripts"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "ingest": "npm run start -w scripts",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down"
  }
}
